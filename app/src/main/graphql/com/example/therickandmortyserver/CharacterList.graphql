query CharacterList {
    characters {
        results {
            id,
            name,
            status,
            species,
            type,
            gender,
            image
        }
    }
}